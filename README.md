# Key-Codes
A web app that displays ASCII values and visual feedback for each key typed. Users type in an input field, and the app shows:  Each key's character and ASCII value in a log, A glowing animated display of recently pressed keys, A clear button to reset everything. Built with HTML, CSS, and  JavaScript.
